# Smart Waste Classifier (Live Feed)

## Overview
AI-based waste segregation system that captures live video and classifies visible waste items using a trained CNN model.

## Workflow
1. Start `app.py`.
2. Live video runs for 10 seconds.
3. CNN model classifies each frame.
4. Matplotlib visualizes category distribution.

## Requirements
Install dependencies:
